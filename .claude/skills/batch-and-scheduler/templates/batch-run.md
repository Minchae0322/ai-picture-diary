# YYYY-MM-DD <작업명> 실행 계획서

1회성 대량 처리(마이그레이션, 재계산, 일괄 보정) 실행 전에 작성한다.
DB 스키마를 바꾸는 부분이 있으면 `docs/db/`의 변경 문서와 `sql/patch/` SQL 파일을 함께 만든다.
**AI는 이 문서와 SQL/코드까지만 만든다. 실행은 사람이 한다.**

## 무엇을 / 왜

- 하려는 것:
- 배경(왜 지금 필요한가):
- 하지 않으면 생기는 문제:

## 대상

| 항목 | 값 |
|---|---|
| 대상 테이블 | |
| 대상 조건 | |
| 대상 건수 (조회 기준일) | |
| 대상 환경 | dev / prod  (스테이징이 있으면 추가) |

## 실행 방식

| 항목 | 값 |
|---|---|
| 진입점 | (관리 API / CLI / SQL 파일) |
| 청크 크기 | |
| 병렬 워커 수 | |
| 분할 기준 | (ID 범위 / 해시 나머지 / 날짜) |
| 예상 소요 시간 | (1% 리허설 기준 외삽값) |
| 실행 시간대 | (트래픽 낮은 시간) |

## 사전 확인

- [ ] 1% 리허설을 dev에서 수행하고 소요 시간을 측정했다
- [ ] 중단 후 재실행이 안전하다(진행 상태 컬럼 또는 처리 조건이 자연 멱등)
- [ ] 대상 행을 다른 트랜잭션이 동시에 건드리지 않는다(또는 락 순서를 정했다)
- [ ] 백업 또는 되돌릴 방법이 있다
- [ ] 관련 배치/스케줄러를 잠시 끌 필요가 있는지 확인했다
- [ ] 인덱스를 나중에 만들지, 미리 만들지 정했다

## 실행 순서

1. dev 실행 및 검증
2. (필요 시) 코드 배포
3. prod 실행
4. 확인 SQL 수행

## 롤백

- 되돌리는 방법:
- 되돌릴 수 없는 지점:
- 되돌리는 데 걸리는 시간:

## 확인 SQL

```sql
-- 처리 전후 건수 비교
-- 샘플 행 확인
```

## 실행 기록 (실행자가 작성)

| 환경 | 실행 일시 | 실행자 | 대상 건수 | 성공 | 실패 | 소요 시간 | 비고 |
|---|---|---|---|---|---|---|---|
| dev | | | | | | | |
| prod | | | | | | | |
