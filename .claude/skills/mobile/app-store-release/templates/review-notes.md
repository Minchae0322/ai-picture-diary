# 심사 노트 (App Store "App Review 정보" / Play Console "앱 액세스" 공통)

`{{...}}`를 프로젝트 값으로 채운다. 채울 값은 프로젝트 스킬(`.claude/skills/<제품>/SKILL.md`)에 있다.

## 데모 계정
- ID: `{{심사용 계정 이메일}}`
- PW: (제출 직전 발급, 심사 끝나면 폐기)
- 로그인 방법: 앱 실행 -> `{{로그인 진입 경로}}` -> 위 계정 입력
- 소셜 로그인만 있는 경우: 심사용 이메일 로그인 경로를 숨겨서라도 제공하거나, 로그인 없이 볼 수 있는 화면을 명시

## 앱 설명 (3~5문장, 구체적으로)
`{{앱 이름}}`은 `{{한 문장 설명 - 누가, 무엇을, 어떻게}}`.
주요 기능: `{{기능 목록. AI를 쓰면 어떤 모델/서비스를 쓰는지까지}}`.

> 심사자는 앱을 처음 본다. "혁신적인" 같은 수식어 대신 **무엇을 누르면 무엇이 나오는지**를 쓴다.

## 이번 제출에서 새로 추가/변경된 것
- (첫 제출이면) 최초 제출
- (업데이트면) `{{추가·변경된 기능. 리젝 대응이면 무엇을 어떻게 고쳤는지}}`

## 사용하는 외부 서비스

| 서비스 | 용도 | 보내는 데이터 |
|---|---|---|
| `{{인증 서비스}}` | 로그인 | `{{예: 이메일}}` |
| `{{LLM 서비스}}` | `{{용도}}` | `{{보내는 본문. 저장 여부도 함께}}` |
| `{{크래시 수집}}` | 크래시 수집 | 기기 정보 |

사용자 생성 콘텐츠를 외부 LLM에 보낸다면 **저장 여부와 학습 사용 여부**를 여기와 개인정보처리방침 양쪽에 쓴다. 심사에서 가장 자주 묻는 항목이다.

## 심사자가 확인해야 할 경로

| 항목 | 경로 |
|---|---|
| 계정 삭제 | `{{예: 마이페이지 > 설정 > 계정 삭제}}` |
| 개인정보처리방침 | `{{앱 안 경로}}` |
| 신고/차단 (UGC 있으면 필수) | `{{경로}}` |
| 인앱 결제 | `{{경로. 샌드박스 계정 안내}}` |

## 권한 사용 이유
- `{{권한}}`: `{{왜 필요한지. "더 나은 경험"은 사유가 아니다}}`

## 연락처
- 이름 / 전화 / 이메일 (심사 중 연락 가능한 것)
