# 일기 API 명세 (핵심 루프 02→03→04)

> 2026-09-21 · 프론트 전달용 · 코드 기준 `api/src/main/java/com/jellydiary/api/diary`

## 공통

- Base URL: `http://<host>:8080`
- 인증: **아직 없음.** 임시로 `X-User-Id: <사용자 ID>` 헤더 필수. 없으면 401 `COMMON_UNAUTHORIZED`.
  (`spring-auth` 적용 시 `Authorization: Bearer`로 교체되며 이 헤더는 삭제된다)
- 성공 봉투: `{ "data": ..., "meta": { "traceId": "..." } }`
- 실패 봉투: `{ "error": { "code", "message", "errors?" }, "meta": { "traceId" } }`
- 날짜: `entryDate`는 `YYYY-MM-DD`. "오늘"의 기준은 서버의 Asia/Seoul 자정.
- ID는 문자열로 내려간다.
- 목록은 커서 페이징. `meta.nextCursor`가 null이면 끝.

### 에러 코드

| code | HTTP | 언제 |
|---|---|---|
| `COMMON_INVALID_REQUEST` | 400 | 본문 누락·501자 이상 등 검증 실패. `errors[]` 동봉 |
| `COMMON_UNAUTHORIZED` | 401 | `X-User-Id` 없음 |
| `COMMON_INVALID_CURSOR` | 400 | 커서 형식 오류 |
| `DIARY_NOT_FOUND` | 404 | 없거나 남의 일기 |
| `DIARY_ALREADY_EXISTS` | 409 | 오늘 기록이 이미 있음 |
| `DIARY_REGENERATE_LIMIT` | 422 | 재생성 상한(3회) 초과 또는 생성 중 |
| `COMMON_INTERNAL_ERROR` | 500 | 그 외 |

### 공통 타입

```ts
type Weather = 'SUNNY' | 'PARTLY_CLOUDY' | 'CLOUDY' | 'RAIN' | 'SNOW' | 'RAINBOW';
type DiaryStatus = 'GENERATING' | 'DONE' | 'FAILED';
```

`moodScore`는 -3 ~ +3 정수. `status`가 `GENERATING`이면 `weather`·`moodScore`·`aiComment`·`imageUrl`은 전부 null이다.
`DONE`인데 `imageUrl`만 null이면 **그림 생성만 실패한 것**이며 화면은 텍스트 결과를 보여준다.

---

## POST /api/v1/diaries - 오늘 기록 (02 → 03)

| 요청 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `content` | string(1~500) | O | 오늘의 한 줄 |
| `userHint` | Weather | X | 빠른 감정 칩 |

성공 **201** + `Location: /api/v1/diaries/{id}`

```json
{ "data": { "id": "12", "status": "GENERATING" }, "meta": { "traceId": "..." } }
```

```bash
curl -X POST http://localhost:8080/api/v1/diaries \
  -H 'Content-Type: application/json' -H 'X-User-Id: 1' \
  -d '{"content":"오늘 팀 회의가 잘 끝나서 마음이 가볍다","userHint":"SUNNY"}'
```

에러: 409 `DIARY_ALREADY_EXISTS`, 400 `COMMON_INVALID_REQUEST`

---

## GET /api/v1/diaries/{id} - 상세 / 생성 폴링 (03, 04)

성공 **200**

```json
{
  "data": {
    "id": "12",
    "entryDate": "2026-08-24",
    "content": "오늘 팀 회의가 잘 끝나서 마음이 가볍다",
    "status": "DONE",
    "weather": "SUNNY",
    "moodScore": 2,
    "aiComment": "AI 코멘트 · 성취감이 느껴지는 하루였네요.",
    "imageUrl": "https://.../600/600",
    "canRegenerate": true
  },
  "meta": { "traceId": "..." }
}
```

폴링: `status`가 `GENERATING`인 동안 1초 간격. `DONE`/`FAILED`가 되면 멈춘다.
에러: 404 `DIARY_NOT_FOUND` (남의 일기도 404)

---

## GET /api/v1/diaries/today - 오늘 기록 유무 (홈 분기)

성공 **200**. 기록이 없으면 `data`가 **null**이다 (404가 아니다).
응답 형태는 상세와 같다. 앱은 이 값으로 02 / 03 / 04를 고른다.

---

## GET /api/v1/diaries - 최근 기록 (02 하단, 커서)

| 쿼리 | 기본 | 설명 |
|---|---|---|
| `cursor` | 없음 | 이전 응답의 `meta.nextCursor` |
| `size` | 20 | 최대 100 |

```json
{
  "data": [
    { "id": "12", "entryDate": "2026-08-23", "weather": "SUNNY", "content": "햇살 가득" }
  ],
  "meta": { "traceId": "...", "nextCursor": "MjAyNi0wOC0yMw", "hasNext": true }
}
```

정렬은 `entryDate` 내림차순 고정. 하루 1건이라 커서는 날짜 하나로 유일하다.

---

## POST /api/v1/diaries/{id}/regenerate - 다시 그리기 (04)

바디 없음. 성공 **200** `{ "id": "12", "status": "GENERATING" }`
에러: 422 `DIARY_REGENERATE_LIMIT`(상한 3회 초과 또는 이미 생성 중), 404 `DIARY_NOT_FOUND`

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-21 | 최초 작성 (핵심 루프 5개 엔드포인트) |
